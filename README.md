# Prereq

```sh
./build.sh
./run.sh
```

```sh
localhost:3000  # Open WebUI
localhost:3000  # Open WebUI
localhost:8080  # Llama-swap
```

## Connect from external pc (on same network)
Check ip:
```sh
ipconfig getifaddr en0
```
then
```sh
<ip>:3000
```